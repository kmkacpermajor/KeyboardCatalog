<Window x:Class="UI_WPF.ManufacturerFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manufacturer Form" Height="200" Width="300">
    <StackPanel Margin="10">
        <TextBlock Text="Manufacturer Form" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBox Text="{Binding Manufacturer.Name, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10"/>

        <Button Content="Save" Command="{Binding SaveCommand}" Width="100" 
                HorizontalAlignment="Right"/>
    </StackPanel>
</Window>
